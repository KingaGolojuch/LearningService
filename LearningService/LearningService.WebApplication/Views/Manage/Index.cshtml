@model LearningService.WebApplication.Models.User.UserViewModel
@{
    ViewBag.Title = "Zarządzanie kontem";
}
@if (!string.IsNullOrEmpty(ViewBag.StatusMessage))
{
    <p class="text-success">@ViewBag.StatusMessage</p>
}

<div class="row">
    <div class="col-md-4">
        @Html.Partial("_LeftSettingMenuOptions")
    </div>

    <div class="col-md-8">
        <h2>Zarządzanie kontem</h2>
        <hr />
        <div class="form-group">
            @Html.LabelFor(model => model.Name, htmlAttributes: new { @class = "control-label" })
            @Html.DisplayFor(model => model.Name)
        </div>
        <div class="form-group">
            @Html.LabelFor(model => model.Surname, htmlAttributes: new { @class = "control-label" })
            @Html.DisplayFor(model => model.Surname)
        </div>
        <div class="form-group">
            @Html.LabelFor(model => model.Email, htmlAttributes: new { @class = "control-label" })
            @Html.DisplayFor(model => model.Email)
        </div>
        informacje o koncie
        <ul>
            <li>imie</li>
            <li>nazwisko</li>
            <li>adres email</li>
            <li>ilość artykułów</li>
            <li>ilość kursów</li>
        </ul>
        <p>do prawej stronie czy lewej mozna by umiescic jakies informacje w stylu : prywatność: czy twój email ma się wyświetlać jako twórca</p>
        <p>np aktywność, tutaj wyświetlić, stworzyłes takie kursy, przeczytałeś takie posty, dostałeś tyle maili</p>
        <p>do przemyślenie zmiana adresu email do korenspondecji (pytanie czy myślimy o sytuacji, kiedy użytkownik nie będzie chciał podawać adresu e-mail do siebie, z drugiej strony niekoneicznie użytkownik musi widzieć jaki adres email jest twórcy kursu i wteyd nei musi odpowiadac na maila)</p>
    </div>
</div>